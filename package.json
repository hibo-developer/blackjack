{
  "name": "02-blackjak",
  "version": "1.0.1",
  "description": "Un juego de Blackjack (21) implementado en JavaScript puro con interfaz web.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cap:sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "android:build": "cd android && gradlew bundleRelease",
    "android:build:clean": "cd android && gradlew clean bundleRelease",
    "android:signing:setup": "powershell -ExecutionPolicy Bypass -File scripts/setup-signing.ps1",
    "android:signing:check": "powershell -ExecutionPolicy Bypass -File scripts/setup-signing.ps1 -DryRun",
    "android:release:signed": "npm run cap:sync && npm run android:build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1"
  }
}
